# 📊 Résumé Visuel : Créer une Feature

## 🎯 Processus en 14 étapes

```
┌─────────────────────────────────────────────────────────────┐
│  1. Créer la structure de dossiers                         │
│     mkdir -p src/features/products/{api,components,...}     │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  2. Types (types/product.types.ts)                          │
│     - Interface Product                                     │
│     - CreateProductDTO, UpdateProductDTO                    │
│     - ProductFilters                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  3. Validators (validators/product.schema.ts)                │
│     - createProductSchema (Zod)                            │
│     - updateProductSchema                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  4. API Client (api/productsApi.ts)                          │
│     - getAll(), getById(), create(), update(), delete()     │
│     - Utilise apiClient de @/shared/api                    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Service Métier (services/productService.ts)              │
│     - ProductService.formatPrice()                          │
│     - ProductService.isAvailable()                          │
│     - Logique métier pure                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  6. Redux Slice (store/productsSlice.ts)                    │
│     - État initial                                          │
│     - Actions (setProducts, addProduct, etc.)               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  7. Thunks (store/productsThunks.ts)                        │
│     - fetchProductsThunk                                    │
│     - createProductThunk                                    │
│     - Actions async                                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  8. Selectors (store/productsSelectors.ts)                   │
│     - selectProducts                                        │
│     - selectAvailableProducts (dérivé)                      │
│     - Selectors memoized                                    │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  9. Hook (hooks/useProducts.ts)                              │
│     - useProducts()                                         │
│     - Connecte Redux à l'UI                                 │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  10. Composants (components/ProductCard/)                   │
│      - ProductCard.tsx                                      │
│      - Composant de présentation                            │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  11. Index.ts (index.ts)                                     │
│      - Barrel export                                        │
│      - API publique du feature                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  12. Ajouter au rootReducer.ts                              │
│      import { productsReducer } from ...                    │
│      products: productsReducer                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  13. (Optionnel) Ajouter endpoints API                     │
│      API_ENDPOINTS.PRODUCTS = { ... }                       │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  14. Créer une page Next.js (app/products/page.tsx)         │
│      Utiliser useProducts() et ProductCard                  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Ordre de création recommandé

### Phase 1 : Fondations (Types et Validation)
1. ✅ Types (`types/product.types.ts`)
2. ✅ Validators (`validators/product.schema.ts`)

### Phase 2 : Data Access
3. ✅ API Client (`api/productsApi.ts`)

### Phase 3 : Business Logic
4. ✅ Service Métier (`services/productService.ts`)

### Phase 4 : State Management
5. ✅ Redux Slice (`store/productsSlice.ts`)
6. ✅ Thunks (`store/productsThunks.ts`)
7. ✅ Selectors (`store/productsSelectors.ts`)

### Phase 5 : Presentation
8. ✅ Hook (`hooks/useProducts.ts`)
9. ✅ Composants (`components/ProductCard/`)

### Phase 6 : Intégration
10. ✅ Index.ts
11. ✅ rootReducer.ts
12. ✅ (Optionnel) Constantes API
13. ✅ (Optionnel) Page Next.js

## 📋 Template rapide

### Commande pour créer la structure
```bash
mkdir -p src/features/products/{api,components,hooks,services,store,types,validators}
```

### Fichiers à créer (dans l'ordre)
```
1. types/product.types.ts
2. validators/product.schema.ts
3. api/productsApi.ts
4. services/productService.ts
5. store/productsSlice.ts
6. store/productsThunks.ts
7. store/productsSelectors.ts
8. hooks/useProducts.ts
9. components/ProductCard/ProductCard.tsx
10. components/ProductCard/index.ts
11. index.ts
```

### Modifications à faire
```
1. src/app/store/rootReducer.ts (ajouter productsReducer)
2. src/shared/lib/constants/api.constants.ts (ajouter PRODUCTS endpoints)
3. app/products/page.tsx (créer une page)
```

## 💡 Astuce

**Copiez la feature `users` comme base** et remplacez :
- `users` → `products`
- `User` → `Product`
- `useUsers` → `useProducts`
- etc.

Puis adaptez selon vos besoins spécifiques !

## 📚 Guide complet

Pour les détails de chaque étape, voir :
**[CREATE_FEATURE_GUIDE.md](./CREATE_FEATURE_GUIDE.md)**



